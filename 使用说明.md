# Top Post Sharing · 每周海报

## 自服务门户（推荐：自己粘贴链接就出图）

不想每次都把链接发给我？用本地门户，**粘贴链接 → 自动生成整张海报**，全程可编辑：

1. 双击 `app/启动生成器.command`（macOS）—— 自动启动本地抓取服务并打开浏览器到 `http://127.0.0.1:8731/`
   - 那个终端窗口**可以随时关掉**，服务在后台继续跑（只监听本机 127.0.0.1，不上公网）
   - 想彻底停掉：终端执行 `pkill -f "app/serve.py"`
2. 把笔记链接粘到页面顶部输入框 —— **粘贴即自动开始**（也可以按回车 / 点「一键生成海报」）
3. 自动完成：账号名 / 头像 / 三个数据 / 封面 → 手机屏 / 二维码 全部填好，
   并按抓取到的真实数据起草「封面 / 文案 / 关键词 / 可复用打法」四段（页面上可直接改）
4. 左侧改文案、右侧实时预览，点「下载 PNG (2x)」导出成品

界面上的状态都会明说：`抓取中…（打开笔记页面 → 解析正文 → 下载封面 → 起草分析）` → `已生成`，
失败时会直接给出原因和下一步建议（例如「链接缺少访问凭证 xsec_token，请在 App 里重新分享复制链接」），
并可点「重试」。上次导入的笔记会记住，重开页面接着编辑。

门户与下方交付的可编辑海报是**同一套引擎**，编辑 / 导出 / 二维码一切都照旧。

## 分析文案怎么「策略化」的

分析口径借鉴了小红书账号诊断 skill 的**反空话约束**：每条结论必须同时具备
①**数据支撑** ②**可落地动作** ③**可验证标准**，禁止「提升内容质量」这类没有动作、没有数据的表述。

所以每段都是同一个结构：**判断 → 数据 → 可复用动作**。举例如下。

| 段落 | 判断（结论先行） | 数据支撑 | 可复用动作 |
|---|---|---|---|
| 封面优点 | 视频笔记·首帧即封面，画面独自承担转化 | 竖版 1.8:1、正文仅 18 字不写参数 | 锁死「主体占画面 2/3 + 1 秒可辨认主角」，竖版优先 |
| 文案优点 | 情绪惊叹式开场 | 正文 18 字 / 标签 10 个，标签信息量 > 正文 | 正文压到 30 字内只放情绪钩子，产品信息交给 8-10 个标签 |
| 关键词 | 覆盖 品牌/明星/场景/流量 四类 | 实际话题标签按四类归好 | 固定「品牌 2 + 场景 1 + 流量 2」配比，别只堆品牌词 |
| 可复用打法 | 把这条笔记沉淀成下周能照做的动作 | 本条收藏率、互动量作为基线 | 首帧单独挑 + 照抄标签配比 + 结尾加提问；验收标准：互动量不低于本条 174 |

**第四段「可复用打法」可以关掉**：链接框底下有个勾选框，取消勾选就只出三段，
海报高度回到原始的 **1099**；勾上则多一段，海报会长高约 110px（约 1209）。
另外自动起草时会剔除账号自己的标签（比如店名/柜哥名做成的话题），避免混进关键词里。

## 每周只需一步（把链接发给我也行）

**把小红书笔记链接发给我**（直接粘贴链接或 App 的分享文案都行），我会：

1. 抓取笔记数据 —— 账号昵称、头像、点赞/收藏/分享数、封面图、标题、正文、话题标签
2. 写固定分析 —— **封面**、**文案**、**关键词**、**可复用打法**
3. 生成一张已填好的海报页面 + 导出的 2x PNG，放在 `输出/` 目录

产出长这样：

```
输出/
  20260910_Lisa宝格丽纽约活动.html   ← 可编辑源（就是海报生成器，数据已填好）
  20260910_Lisa宝格丽纽约活动.png    ← 1080×2198 成品图，可直接发布
```

## 分析和文案要改怎么办

我写的三段分析是判断，可能不合你的意。两种改法：

- **小改**：直接打开 `输出/xxx.html`，左边改文案、右边实时预览，改完点「下载 PNG」
- **让我改**：把想改的地方告诉我，我重新生成

改过的内容会按笔记 ID 单独存在浏览器里，不会影响其他周的海报。
（门户页同理：每篇笔记用独立的 `tps_portal_<noteId>` 存储键，互不串台；
想从头再来，用「更多 → 恢复为示例」清掉本地草稿。）

## 自动填入 vs 需要确认

| 项目 | 来源 |
|---|---|
| 头像 | 自动（笔记作者头像） |
| 账号名 | 自动（笔记作者昵称，前面加 @） |
| 三个数据 | 自动（点赞 / 收藏 / 分享，可在左侧改成评论） |
| 手机屏里的图 | 自动（笔记封面，按手机屏比例居中裁剪） |
| 二维码 | 自动（就是这篇笔记的链接，扫出来直达） |
| 卡片里的四段分析 | **自动起草，需要你过目**（数字与标签都来自真实抓取，判断以你为准） |

## 分享给同事（三种形态，按场景挑）

点页面右上角「更多 → **分享给同事…**」会直接给出当前可用的地址，不用记命令。

| 形态 | 同事要做什么 | 能否自动抓取 | 适合 |
|---|---|---|---|
| **A. 团队共享**（最推荐） | 浏览器打开一个网址 | ✅ | 办公室同一 WiFi；一个人开服务，全组用 |
| **B. 人手一份**（本地包） | 解压 → 双击启动器 | ✅ | 远程 / 在家办公；每人各自跑 |
| **C. 离线单文件** | 双击打开 HTML | ❌ 手动填 | 非 Mac、临时应急、微信直传 |

**A. 团队共享**（同事零安装，最省事）

```bash
cd TopPostSharing/app
TPS_HOST=0.0.0.0 python3 serve.py
```

启动后终端会打印同事入口，类似 `http://172.19.0.1:8731/`，发给同事即可（需同一局域网）。
默认的 `127.0.0.1` 模式只在本机可用，不会对局域网暴露。

**B. 人手一份**（打成一个 zip 发出去）

```bash
python3 app/build_share.py     # 产出 分享版/TopPostSharing-分享版.zip
```

zip 里就是同事需要的一切，**纯 Python 标准库，不用 pip install 任何东西**：

```
TopPostSharing-分享版/
  ① 双击我启动.command          ← 同事只需双击这个（自动找 python3 并开浏览器）
  index.html                    ← 门户页（库已内联）
  serve.py / scrape.py / fetch_note.py
  离线备用·无需服务.html          ← 没装 Python 时的降级方案
  使用说明（同事版）.md
```

已验证：用 macOS **自带的 python3.9** 解压后直接跑通全链路（抓取 → 填版 → 导出），零依赖。

**C. 离线单文件**：`每周海报生成器.html` —— 双击就用，改字/加行/导出 PNG 全都有，只是不自动抓链接。

> 小提醒：macOS 会给外部下载的文件加隔离标记，同事首次双击 `.command` 若提示「身份不明的开发者」，
> 右键 → 打开 即可；微信传输有时会丢可执行权限，这时在终端执行一次
> `chmod +x ①\ 双击我启动.command` 就行（同事版说明里写了这一步）。

## 目录里还有什么

- `app/` —— **自服务门户**：`启动生成器.command`（双击启动）、`serve.py`（本地服务）、`index.html`（门户页）、`scrape.py`（抓取 + 策略化起草）、`build_portal.py`（生成门户页）、`build_share.py`（打包分享版）
- `分享版/` —— 给同事的成品包（文件夹 + zip），`python3 app/build_share.py` 可随时重新生成
- `每周海报生成器.html` —— 空白工具，手动填内容用（临时做别的图时用得上）
- `_build/` —— 出图流水线，平时不用管

## 流水线（想自己跑的时候）

```bash
cd TopPostSharing/_build
PY=/Users/wutingxiang/.workbuddy/binaries/python/versions/3.13.12/bin/python3

# 1. 抓笔记（链接可以是分享文案，脚本会自己挑出 URL）
$PY fetch_note.py "<链接>" note_out

# 2. 改 note_out/../analysis.json 里的三段分析（或让我写）

# 3. 生成海报
$PY make_poster.py --note note_out/note.json --analysis analysis.json

# 4. 校验几何 + 导出 PNG + 解码二维码自检
$PY verify.py "../输出/xxx.html"
/Users/wutingxiang/.workbuddy/binaries/python/envs/default/bin/python export.py "../输出/xxx.html" "../输出/xxx.png"
```

## 版式参数（别乱动，是照着参考图逐像素对过的）

- 设计稿宽 540px，导出 2x = 1080px，海报总高恒为 **1099**（内容多会自动长高）
- 卡片 `min-height:522px`，二维码用 flex 顶到底部，保证每周比例一致
- 指标图标描边 **#C00000**（深红），卡片标签底色 #FB304E
- 二维码 **93px** 模块区 + 6px 白边 = 外框 107px

## 已知限制 / 排错

- 需要登录才能看的笔记（仅粉丝可见、已删除）抓不到
- 抓取依赖分享链接里的 `xsec_token`：**链接必须从 App 的「分享 → 复制链接」拿**，
  浏览器地址栏里直接复制的、或从别处转发的短链常常会 404
- 页面上出现「本地抓取服务未连接」：说明抓取服务没在跑，双击 `app/启动生成器.command` 再点「重新连接」
- 排错日志在 `app/server.log`（每次抓取的成功/失败原因都有记录）
- 图文笔记有多张图时，第 2 张会自动放到悬浮小图位；视频笔记只有封面，悬浮小图位会隐藏
- 分析文案是自动起草的，涉及品牌/明星的判断请以你为准
- 勾选「＋可复用打法」时海报会从 1099 长到约 1209（多一段），想保持原始比例就取消勾选
- 团队共享模式（`TPS_HOST=0.0.0.0`）会把服务暴露给**同一局域网**内的其他人，公共网络下别开
